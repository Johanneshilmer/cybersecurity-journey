Tools of the Trade: Linux and SQL

Modul 1
OS (Operationsystem)
Det är användargränsnittet mellan datorns hårdvara och användaren.
OS kopplar samman användarem applikationer och hårdvara, detta gör att vi kan utföra uppfiter på datorn.
  Fyra steg för att utföra en uppgift
    - Användaren – initierar en uppgift (t.ex. vill spara en fil).
    - Applikationen – programmet användaren interagerar med (t.ex. webbläsare eller kalkylator).
    - Operativsystemet – tolkar begäran och skickar den vidare till rätt hårdvara.
    - Hårdvaran – utför uppgiften (CPU beräknar, hårddisk lagrar, osv.) och skickar resultatet tillbaka via OS → applikation → användare.

  Exempel – Ladda ner en fil
    - Användaren klickar på ”Ladda ner” i webbläsaren.
    - Webbläsaren skickar begäran till OS.
    - OS skickar den vidare till hårdvaran.
    - Hårdvaran laddar ner filen, OS skickar info tillbaka till webbläsaren som visar att nedladdningen är klar.

Två olika interface som användaren kommer i kontakt med.
  - GUI (Graphical User Interface)
    Är det vanliga UI man ser vid applikationer, innehåller oftast start menu, task bar, desktop ikon.
  - CLI (Command-Line Interface)
    Är kommando fönstret som man ofta använder på linux, det är test baserat då man använder det genom att skriva olika kommandon.




Modul 2
- Linux-arkitektur förklarad -
Att förstå Linux-arkitekturen är viktigt för en säkerhetsanalytiker eftersom det gör det lättare att förstå hur systemet fungerar. 
En uppgift i Linux passerar genom flera lager: användaren → applikationer → skalet → FHS (Filesystem Hierarchy Standard) → kärnan → hårdvaran.

Användare: 
  Den som interagerar med datorn. Linux är ett multi-user-system där flera kan använda resurser samtidigt.

Applikationer: 
  Program som utför specifika uppgifter. I Linux hanteras installation och uppdatering ofta via en pakethanterare.

Shell (skal): 
  Ett textbaserat gränssnitt där användaren skriver kommandon. Skalets roll är att översätta kommandon till kärnan.

Filesystem Hierarchy Standard (FHS): 
  Bestämmer hur filer och kataloger organiseras så att systemet vet var data lagras.

Kärnan (kernel): 
  Hanterar processer, minne och kommunikation mellan applikationer och hårdvara. Kärnan styr systemresurser och hårdvarans funktioner.

Hårdvara: 
  De fysiska komponenterna i datorn.

Perifer utrustning: 
  Externa enheter som tangentbord, mus, skärm och skrivare.

Intern hårdvara: 
  Komponenter som krävs för att datorn ska fungera, bl.a.

CPU: 
  Utför beräkningar och kör program.

RAM: 
  Temporärt minne som lagrar data under tiden en uppgift körs. Innehållet försvinner när datorn stängs av.

Hårddisk: 
  Långtidsminne där filer och program sparas permanent.


Linux shell 
  Shell är det som översätter vad du skriver i CLI till maskinen




Modul 3
Olika kommandon att använda.
🔹 Kommando
Ett kommando är själva programmet eller instruktionen du vill köra.
Exempel:
        ls
Här är ls kommandot, som betyder "lista filer och mappar".

🔹 Argument
Ett argument är extra information du skickar till kommandot för att tala om hur det ska köras eller vilket objekt det ska köras på.
Exempel:
        ls Documents
ls = kommandot
Documents = argumentet (säger till ls vilken katalog som ska listas).

🔹 Flaggor/optioner (en typ av argument)
Argument kan också vara flaggor som ändrar beteendet på kommandot.
Exempel:
        ls -l
ls = kommandot
-l = ett argument (flagga) som säger: "visa detaljerad lista".

👉 Så:
Kommando = vad du vill göra
Argument = på vad eller hur du vill göra det

Olika användbara kommandon
pwd = visar vilken directory vi är i.
ls = lisar alla mappar och filer i de directory man är i.
cd mapp-namn = för att "change directory" att man går in i den mappen.
cd .. = får tillbaka en directory.
cat fil-namn = visar filens innehåll.
head fil-namn = visar 10 första raderna.
tail fil-namn = visar 10 sista raderna.

Hitta och filtrera data
grep namn fil-namn = söker efter allt som innehåller "namn" i filen fil-namn.
piping "|" används för att sätta ihop flera kommandon, 
Exempel:
    ls /home/analyst/reports | grep users
Detta söker bara i /home/analyst/reports directory och outputar allt som innehåller users.

find
Kommandot find används för att söka efter filer och kataloger baserat på olika kriterier, t.ex. namn, storlek eller senaste ändringsdatum.
  -name: sök efter namn (skiftlägeskänsligt).
  -iname: sök efter namn (inte skiftlägeskänsligt).
  -mtime: sök efter filer ändrade inom en viss tidsram (baserat på dagar).
  -mmin: samma som ovan men baserat på minuter.
Exempel:
        find /home/analyst/projects -iname "*log*"
→ hittar alla filer i projects vars namn innehåller "log", oavsett stora eller små bokstäver.
        find /home/analyst/projects -mtime -3
→ listar filer/kataloger som ändrats de senaste 3 dagarna.

Skapa och ta bort directory
mkdir = skapar ett directory
rmdir = tar bort ett directory
touch = skapar fil
rm = deletar en fil
mv = flyttar en fil, exempel, mv test.txt /home/dokument/tester/ flyttar vu den till tester directory
cp = copy en fil eller directory


Det finns tre olika permissions i linux
  👉 Read
  👉 Write
  👉 Execute

Def finns tr olika owners
  👉 User = Ägaren av filen
  👉 Group = Beståv av flera users
  👉 Other = Alla andra användare

Fil permissions prepresenteras med 10 figurer.
  👉drwxrwxrwx = full permission för en fil.
      d → det är en directory (mapp)
      rwx (User) → ägaren kan läsa, skriva och gå in i mappen
      rwx (Group) → gruppen kan läsa, skriva och gå in i mappen
      rwx (Other) → alla andra kan läsa, skriva och gå in i mappen

Att lista filer och permissions
👉ls -l
All list gömda filer
👉ls -a
Att lista gömda filer och permissions
👉ls -la

Hur man ändrar permissions 👉chmod
chmod g+w, o-r access.txt = "G"roup kan "W"rite och tar bort "R"ead från "O"thers för filen med namn access.txt


Add och Delete users
  Root user (superuser) = Har kontroll över hela systemet och kan göra ändringar på det.
  useradd namn = lägger till en användare i systemet.
  userdel namn = tar bort en användare från systemet. Man får ett "fel" meddelande efter så man ska göra "groupdel" för att städa upp
  groupdel namn
  usermod -g team namn = Addar en user till ett team (grupp)
  chown namn fil = ändrar ägarskap till en annan
  usermod -a -G nyteam namn = lägger till user i en till grupp, -G = för att lägga till i en till grupp, -a = ansluter user till gruppen.

Att få mer information om ett kommand kan man använda
  man kommandonamnet exempel: man chmod
Vill man få fram information om hur man kanske byter lösnord, kan man söka efter keywords och få fram information
  apropos password
Man kan också få in flera stringar i sökningen med 
  apropos -a change password





Modul 4
Databaser
En databas är ett organiserat system för att lagra, hantera och hämta data. 
Den används för att strukturera information så att den kan sökas, uppdateras och analyseras på ett effektivt sätt.
Primary key (primärnyckel)
  En kolumn (eller kombination av kolumner) som unikt identifierar varje rad i en tabell.
  Värdet kan inte vara tomt (NULL) och får inte upprepas.
  Exempel: personnummer eller kundID.
Foreign key (främmande nyckel)
  En kolumn i en tabell som refererar till primary key i en annan tabell.
  Skapar relationer mellan tabeller, t.ex. koppla en kund till sina ordrar.
  Säkerställer referensintegritet (att relationer mellan tabeller är korrekta).
Exempel:
  Tabell Kunder: kundID (primary key).
  Tabell Order: orderID (primary key), kundID (foreign key → kopplar ordern till en kund).

SQL (Structured Query Language)
SQL är ett programeringsspråk som används för att arbeta med databaser. Det låter dig skapa, hämta, uppdatera och radera data.
En Query är en instruktion till DB för att hämta eller bearbeta data, en Query är ett kommando som SELECT, INSERT, UPDATE eller DELETE.
De vanligaste delarna är:
  CREATE – skapa tabeller och databaser.
  INSERT – lägga till data.
  SELECT – hämta data.
  UPDATE – ändra data.
  DELETE – ta bort data.

Exemple:
        -- Skapa tabell
        CREATE TABLE Kunder (
          kundID INT PRIMARY KEY,
          namn VARCHAR(50),
          stad VARCHAR(50)
        );
        
        -- Lägg till kund
        INSERT INTO Kunder (kundID, namn, stad)
        VALUES (1, 'Anna', 'Uppsala');
        
        -- Hämta data
        SELECT * FROM Kunder;
        
        -- Uppdatera data
        UPDATE Kunder SET stad = 'Stockholm' WHERE kundID = 1;
        
        -- Ta bort data
        DELETE FROM Kunder WHERE kundID = 1;

Filtrera data.
  Operator är en symbol eller keyword som representerar en "operator", som att söka efter ett specifikt land exepmel: country="sweden"
    SELECT * FROM LOGIN WHERE country="sweden";
  Vill man specifisera börjav av en mening bara kan man använda "%" exemple:
    SELECT * FROM LOGIN WHERE username LIKE "jo%"
        Svar: johannes, johan, jonte ....
  "%" kan också användas före och efter, vill man endast ha en bokstav till kan man använda "_" exemple:
    SELECT * FROM LOGIN WHERE country LIKE "US_"
        Svar: USA
""
SELECT kolumn FROM table
""

Vi kan också använda oss av "less then", "greater then" samma som vi använder "=" som är "equal to".
  Här har vi ett exempel på "greater then"  
    SELECT firstname, lastname, birthdate
    FROM employees
    WHERE birthdate > '1970-01-01';

              < less than
              
              > greater than
              
              = equal to
              
              <= less than or equal to
              
              >= greater than or equal to
              
              <> not equal to

"BETWEEN AND" = vi kan också använda between och AND för att söka exempel mellan två datum.
  Exemple:
      SELECT firstname, lastname, hiredate
      FROM employees
      WHERE hiredate BETWEEN '2002-01-01' AND '2003-01-01';

Att söka med flera saker med OR
  Exemple:
    SELECT firstname, lastname, country
    FROM customers
    WHERE country = "Argentina" OR country = "Brazil";


Att söka allt förutom med WHERE NOT
  Exemple:
    SELECT *
    FROM customers
    WHERE NOT status = 'successful';

    SELECT firstname, lastname, email, country
    FROM customers
    WHERE NOT country = 'Canada' AND NOT country = 'USA';



--JOIN tables in SQL--
🧩 Varför JOIN?
Tabeller i en databas är ofta relaterade till varandra via en nyckel (t.ex. ett id).
Exempel:
  - Tabell employees: information om anställda.
  - Tabell departments: information om avdelningar.
    Varje anställd har en department_id som pekar på vilken avdelning personen tillhör.

🔑 Vanliga typer av JOIN
1. INNER JOIN (vanligast)
  Hämtar bara de rader som matchar i båda tabellerna.
      SELECT employees.name, departments.department_name
      FROM employees
      INNER JOIN departments 
          ON employees.department_id = departments.id;
  👉 Ger bara de anställda som har en avdelning.

2. LEFT JOIN (eller LEFT OUTER JOIN)
  Hämtar alla från vänstra tabellen, även om det inte finns någon match i den högra.
      SELECT employees.name, departments.department_name
      FROM employees
      LEFT JOIN departments 
          ON employees.department_id = departments.id;
  👉 Alla anställda visas, men om de saknar avdelning blir det NULL i kolumnen department_name.

3. RIGHT JOIN
  Tvärtom mot LEFT JOIN – hämtar alla från högra tabellen, även om det inte finns någon match i vänstra.

4. FULL OUTER JOIN
  Hämtar allt från båda tabellerna, matchande eller inte. (OBS: alla databaser stöder inte detta, t.ex. MySQL gör det inte direkt).





































