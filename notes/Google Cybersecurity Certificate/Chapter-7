Automate Cybersecurity Task with Python

Module 1 Intro till Python

Python och programmering:
  Programmering innebär att ge datorer instruktioner för att utföra uppgifter. Datorer kommunicerar egentligen i binärkod (0 och 1), 
  men programmeringsspråk som Python gör det enklare för människor att skriva kod med mindre och mer förståelig syntax.

  Python i cybersäkerhet används ofta för att automatisera repetitiva uppgifter, exempelvis:
    👉Logganalys
    👉Malwareanalys
    👉Hantering av accesskontroller
    👉Intrångsdetektion
    👉Regelefterlevnad (compliance checks)
    👉Nätverksskanning

# Skriver ut hello python
print("Hello Python")

Data typer:
  String:
    "updates needed"
    "20%"
    "5.0"
    "35"
    "**/**/**" 
    ""

  List:
    [12, 36, 54, 1, 7]
    ["eraab", "arusso", "drosas"]
    [True, False, True, True]
    [15, "approved", True, 45.5, False]
    []

  Integer:
    -100 
    -12
    -1
    0
    1
    20
    500 

  Float:
    -2.2
    -1.34
    0.0
    0.34 

  Boolean:
    True
    False

  Tuple:
    ("wjaffrey", "arutley", "dkot")
    (46, 2, 13, 2, 8, 0, 0)
    (True, False, True, True)
    ("wjaffrey", 13, True)

  Set:
    {"jlanksy", "drosas", "nmason"}

  Dictionary:
    { 1: "East",
    2: "West",
    3: "North",
    4: "South" }

  Att se vilken type en variable har använder vi type()
  username = "johannes"
  print(type(username))
  output => <class 'str'> "alltså string"

Assign och reassign variables i Python
  # Assign "username"
  username = "nzhao"
  print(username)
  output => nzhao

  # ReAssign "username"
  username = "johannes"
  print(username)
  output => johannes

Olika sätt att loopa

For loop nummer
  i = 0
  for i in range(11):
    print(i)

  for i in range(0, 5):
    print(i)


For loop lista
  list = ["banan", "äpple", "mango"]
  for x in list:
    print(x)

While loops

  i = 0
  while i < 5:
      print(i)
      i += 1

Kontrollera loop-exekvering
  👉break: avbryter loopen helt när ett villkor uppfylls.
  👉continue: hoppar över den aktuella iterationen och fortsätter med nästa.

Kort sagt:
  👉for → iterera genom en sekvens.
  👉while → upprepa baserat på ett villkor.
  👉break/continue → styra exekveringen.
  👉Undvik oändliga loopar om du inte vill att koden ska köra för evigt.

Olika operator och dess mening
> greater than

< less than

>= greater than or equal to

<= less than or equal to

== equal to

!= not equal to



if statements
Ett if-statement används för att låta datorn fatta beslut. Det kontrollerar om ett villkor är sant eller falskt och kör kod endast om villkoret är sant.
  Här har vi olika if statements med if, if else, if else elif, if and, if not

  if status == 200:
      print("OK")


  if status == 200:
      print("OK")
  else:
      print("check other status")
  
  
  
  if status == 200:
      print("OK")
  elif status == 400:
      print("Bad Request")
  elif status == 500:
      print("Internal Server Error") 
  
  
  if status == 200:
      print("OK")
  elif status == 400:
      print("Bad Request")
  elif status == 500:
      print("Internal Server Error")
  else:
      print("check other status")
  
  
  if status >= 200 and status <= 226:
      print("successful response")
  
  
  if not(status >= 200 and status <= 226):
      print("check status")










Module 2 Write effective Python code

Basic python functions
  Python-funktioner används för att återanvända kod och effektivisera repetitiva uppgifter, 
  vilket är särskilt användbart i cybersäkerhet där man ofta behöver analysera loggar och upptäcka mönster som misstänkta inloggningsförsök.


Built in function
  Är funktioner som kommer som default i python som tillexempel print(), type(), sorted() och max()

User-defined functions
  Är funtioner som användaren själv har designat.


👉Exempel på en funktion
def greet_employee():
  print("Welcome! You're inlogged in.")

👉Kallar vi på vår funtion
greet_employee()


Argument
  Är data som tillsätts till en funktion när funktionen kallas.

👉Exempel på en funktion med parametrar
def greet_employee(name):
  print("Welcome!", name)

👉Kallar vi på vår funtion med ett argument
greet_employee("Johannes")



Return statement
  Är statement som exekverar inom en funktion och som skickar ut infromationen när funktionen kallas.

👉Exempel på en funktion med statement
def calculate_fails(total_attempts, failed_attemts):
  fail_percentage = failed_attemts / total_attempts
  return fail_percentage

👉Kallar vi på vår funtion med statements
calculate_fails(4,2)
output => 0.5

👉Vi kan också spara output i en variable som detta exempel:
percentage = calculate_fails(4,2)


I Python används parametrar (variabler i funktionsdefinitionen) och argument (värden som skickas in vid funktionsanrop) för att arbeta med data i funktioner. 
För att returnera resultat används return-satser, vilket gör det möjligt att lagra och återanvända värden i andra delar av koden.

Variabler i funktioner kan vara:
  👉Globala variabler – definieras utanför funktioner och kan nås i hela programmet.
  👉Lokala variabler – skapas inuti funktioner och existerar bara medan funktionen körs.

Parametrar och andra variabler i en funktion är alltid lokala. Om en lokal variabel har samma namn som en global, kommer funktionen att använda den lokala versionen, 
vilket kan skapa förvirring.
👉Bästa praxis: undvik att blanda globala och lokala variabler. Använd parametrar för att skicka in data i funktioner i stället för globala variabler.


Python standard modules and libraries
  En modules är en Python-fil som innehåller ytterligare funktioner, variabler och andra typer av körbar kod. Ett Python-libraries är en samling moduler.
  I Python kan du använda moduler (enskilda filer med funktioner, variabler och klasser) och bibliotek (samlingar av moduler) för att återanvända färdig kod.
  Python Standard Library innehåller många färdiga moduler, t.ex.
    👉re för mönstersökning i loggar
    👉csv för arbete med CSV-filer
    👉glob och os för kommandoradsinteraktion
    👉time och datetime för tidsdata
    👉statistics för beräkningar som medelvärde (mean()) och median (median())
  Importera moduler:
    👉import modulnamn – importerar hela modulen (t.ex. import statistics → använd statistics.mean())
    👉from modulnamn import funktion – importerar en specifik funktion (t.ex. from statistics import mean, median → använd mean() direkt)
  Externa bibliotek: Måste installeras innan de kan importeras. Exempel:
    👉BeautifulSoup för HTML-parsning
    👉NumPy för matematiska beräkningar och arrayer
    👉Installering görs via t.ex. %pip install numpy, därefter kan man importera med import numpy.
👉 Kort sagt: Standardbiblioteket ger färdig kod direkt i Python, medan externa bibliotek måste installeras. Import görs antingen av hela moduler eller av enskilda funktioner.


För att skriva läsbar och korrekt Python-kod är det viktigt att följa PEP 8-stilguiden.
  Kommentarer:
    👉Förklara vad koden gör, både i början och i mer komplexa delar.
    👉Enradiga kommentarer börjar med # och bör hållas under 79 tecken.
    👉Fleradiga kommentarer kan skrivas med flera #-rader eller som docstrings (""" ... """).
  Indentering:
    👉Python kräver indentering för funktioner, loopar och villkor.
    👉PEP 8 rekommenderar fyra mellanslag per nivå.
    👉Exempel: en if inuti en while får totalt åtta mellanslag.
  Syntax:
    👉Vanliga fel gäller datatyper, rubriker och kolon.
    👉Strängar ska ha citattecken, siffror och boolska värden ska inte.
    👉Listor skrivs inom hakparenteser med komma mellan elementen.
    👉Funktioner, loopar och villkor måste avslutas med kolon (:).
👉 Kort sagt: Följ PEP 8, kommentera tydligt, använd korrekt indentering och var noga med syntaxen för att undvika fel och göra koden lättare att förstå.








Module 3 Work with strings and lists

I cybersäkerhet är strings en av de vanligaste datatyperna i Python, eftersom de används för data som IP-adresser, användarnamn, URL:er och ID:n.
  Index och bracket notation:
    👉Varje tecken i en string har ett index (0-baserat, kan vara negativt från slutet).
    👉[] används för att extrahera tecken eller delar av en string (slicing).
    👉Exempel: "h32rb17"[0] → "h", "h32rb17"[0:3] → "h32".
  Stringfunktioner och metoder:
    👉str() → konverterar värden till string.
    👉len() → returnerar antal tecken i string.
    👉.upper() / .lower() → returnerar en kopia av strings i stora respektive små bokstäver.
    👉.index() → returnerar index för första förekomsten av ett tecken eller substring; ger fel om det inte finns.
  Praktisk användning i cybersäkerhet:
    👉Extrahera delar av IP-adresser eller ID:n.
    👉Verifiera att användarnamn eller enheter uppfyller kriterier.
    👉Hitta substrings för att analysera loggar eller aktivitet.
👉Kort sagt: Kunskap om strings, indexering, slicing och metoder som .upper(), .lower() och .index() är viktiga verktyg för en säkerhetsanalytiker.






Module 4 Python in practice






